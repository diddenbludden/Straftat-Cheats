<Window x:Class="SharpMonoInjector.Gui.Views.LaunchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SharpMonoInjector.Gui.Views"
        xmlns:vm="clr-namespace:SharpMonoInjector.Gui.ViewModels"
        mc:Ignorable="d"
        Title="Straftard Launcher" Height="300" Width="400"
        Background="#FF2C2C2C" WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">

    <Window.Resources>
        <!-- Visual Effects -->
        <DropShadowEffect x:Key="NeonGlow" Color="#3d598f" BlurRadius="18" ShadowDepth="10" Opacity="0.3"/>
        <DropShadowEffect x:Key="SoftShadow" Color="White" BlurRadius="100" ShadowDepth="1" Opacity="0.1"/>

        <!-- Button Style -->
        <Style x:Key="LaunchButton" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="#FF384953"/>
            <Setter Property="Foreground" Value="#FF4D6999"/>
            <Setter Property="BorderBrush" Value="#3d598f"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontFamily" Value="Arial Black"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Effect" Value="{StaticResource NeonGlow}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter" 
                                              Focusable="False" 
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              Margin="{TemplateBinding Padding}" 
                                              RecognizesAccessKey="True" 
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" 
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="Background" Value="#FF506578"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF5A7CB8"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#3d598f" BlurRadius="20" ShadowDepth="0" Opacity="1.0"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter TargetName="border" Property="Background" Value="#FF394A53"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF2C628B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Window.DataContext>
        <vm:MainWindowViewModel InjectClassName="Loader" InjectMethodName="Init" InjectNamespace="straftard" AVAlert="True"/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Main Launch Button -->
        <Button Grid.Row="0" 
                Content="LAUNCH" 
                Style="{StaticResource LaunchButton}" 
                Margin="50" 
                Command="{Binding Path=LaunchCommand}"
                VerticalAlignment="Center" 
                HorizontalAlignment="Center"/>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2" Background="#FF2A2A2C" Height="30">
            <StatusBarItem Foreground="#FF21ACd0">
                <TextBlock Text="Status:" FontFamily="Arial Black">
                    <TextBlock.Effect>
                        <DropShadowEffect/>
                    </TextBlock.Effect>
                </TextBlock>
            </StatusBarItem>
            <StatusBarItem Foreground="#FF1FA0fC" FontFamily="Arial Black">
                <TextBlock Text="{Binding Path=Status}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>